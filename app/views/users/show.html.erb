<h1><%= current_user.username %>'s Profile</h1>

<br />
<%= button_to '🌱View all gardens🌱', user_gardens_path(@user), method: :get, class: 'btn' %>
<br />
<br />

<%= button_to '🍃Till new garden🍃', new_user_garden_path(@user), method: :get, class: 'btn' %>
<br />
<br />

<%= button_to '🪴View all plants🪴', plants_path, method: :get, class: 'btn' %>
<br />
<br />

<%= button_to '📣Tell us about a new plant📣', new_plant_path, method: :get, class: 'btn' %>
<br />
<br />

<%= button_to '🧤Plant something in a garden🧤', new_garden_plant_path, method: :get, class: 'btn' %>
<br />
<br />
